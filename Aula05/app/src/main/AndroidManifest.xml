<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schem   as.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Aula05">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Aula05"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="ResultadoAbaixoDoPesoActivity" />
        <activity android:name="ResultadoAcimaDoPesoActivity" />
        <activity android:name="ResultadoNormalActivity" />
        <activity android:name="ResultadoObesidade01Activity" />
        <activity android:name="ResultadoObesidade02Activity" />

    </application>

</manifest>